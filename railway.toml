# railway.toml
# BetIntel — Railway deployment config
# Python FastAPI service — do NOT set a Node startCommand here

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 15
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
