[phases.setup]
pkgs = ["python311", "gcc", "libpq"]

[phases.install]
cmds = ["pip install --no-cache-dir -r mlb/requirements.txt"]

[start]
cmd = "python mlb/startup.py && uvicorn mlb.api.main:app --host 0.0.0.0 --port $PORT"
