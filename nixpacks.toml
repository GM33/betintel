[variables]
NIXPACKS_NO_NODE = "1"

[phases.setup]
nixPkgs = ["python311", "gcc", "libpq"]

[phases.install]
cmds = ["pip install --no-cache-dir -r mlb/requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "uvicorn mlb.api.main:app --host 0.0.0.0 --port $PORT"
